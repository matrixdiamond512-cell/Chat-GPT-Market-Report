name: Inspect JPX dynamic pages once
on:
  workflow_dispatch:
  push:
    branches: [main]
    paths:
      - ".github/workflows/inspect-jpx-dynamic-pages-once.yml"
      - "scripts/inspect_dynamic_jpx_pages.py"
permissions:
  contents: write
jobs:
  inspect:
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0
      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"
      - run: python -m pip install --disable-pip-version-check -r scripts/requirements-nikkei225-supply-demand.txt
      - run: python scripts/inspect_dynamic_jpx_pages.py
      - run: python -m json.tool data/jpx-dynamic-page-inspection.json > /dev/null
      - shell: bash
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add data/jpx-dynamic-page-inspection.json
          git commit -m "Inspect JPX dynamic participant pages" || exit 0
          git pull --rebase origin main
          git push origin main

name: Inspect Nikkei 225 session rows once
on:
  workflow_dispatch:
  push:
    branches: [main]
    paths:
      - ".github/workflows/inspect-nikkei225-session-rows-once.yml"
      - "scripts/inspect_nikkei225_session_rows.py"
permissions:
  contents: write
jobs:
  inspect:
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0
      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"
      - run: python -m pip install --disable-pip-version-check -r scripts/requirements-nikkei225-supply-demand.txt
      - run: python scripts/inspect_nikkei225_session_rows.py
      - run: python -m json.tool data/nikkei225-session-row-inspection.json > /dev/null
      - shell: bash
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add data/nikkei225-session-row-inspection.json
          git commit -m "Inspect Nikkei 225 session rows" || exit 0
          git pull --rebase origin main
          git push origin main

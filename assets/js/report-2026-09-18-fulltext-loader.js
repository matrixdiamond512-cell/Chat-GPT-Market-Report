/* Preserve the complete report in its own JSON file; do not summarize. */
(()=>{const original=window.fetch.bind(window);window.fetch=async function(input,init){const url=String(input instanceof Request?input.url:input);if(!/(?:^|\/)reports\.json(?:\?|$)/.test(url))return original(input,init);const response=await original(input,init);let payload;try{payload=await response.clone().json()}catch{payload=[]}const list=Array.isArray(payload)?payload:Array.isArray(payload?.reports)?payload.reports:[];const full=await original('reports/2026-09-18_21-00.json?ts='+Date.now(),{cache:'no-store'}).then(r=>{if(!r.ok)throw Error('Full report HTTP '+r.status);return r.json()});const merged=[...list.filter(r=>!(r.date===full.date&&r.time===full.time)),full];return new Response(JSON.stringify(Array.isArray(payload)?merged:{...payload,reports:merged}),{status:200,headers:{'Content-Type':'application/json'}})};})();